import { Typography } from '@mui/material';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <div>
      <Typography>Hello World</Typography>
    </div>
  );
};

export default Home;
